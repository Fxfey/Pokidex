'use client';
import styled from 'styled-components';

export const ProfileStatStyled = styled.p`
    background-color: var(--white-2);
    color: var(--black-1);
    width: 200px;
    display: flex;
    justify-content: space-between;
    border-radius: 4px;
    padding: 15px 10px;
`;
